# PRD：PlayerBar 纽扣式内凹播放按钮

日期：2026-08-29  
状态：设计方向已确认，待实现与局部视觉确认。  
配套实现说明：[TECHDOC](./TECHDOC-playerbar-matte-depth-2026-08-29.md)。

> 本文明确取代此前“浅凸哑光播放按钮”方案。保留原文件名仅为稳定引用，本文是新的设计定义，不代表已实现或通过视觉验收。

## 1. 目标与核心理解

modern 底部 PlayerBar 的主播放按钮改为“纽扣式内凹”实体按键。
按钮自身应像一枚带凹面的彩色纽扣：同色外沿略高，中央表面向内凹，播放或暂停图标位于中央。
这不是把整个平面按钮埋进播放条的洞里，也不画纽扣孔、细描边或一圈独立亮光环。
静止状态就必须明确读出内凹结构，不能只把旧的线性渐变方向反转后当作完成。
目标是增加克制、可按压的材质层次，同时保持播放控件的颜色身份、清晰度和布局稳定。

## 2. 范围与不变项

仅调整主窗口底部 PlayerBar 的 modern 主播放按钮表面及其局部交互反馈。

- 圆形按钮保持 40×40px；图标尺寸、中心位置、点击区域和相邻控件布局不变。
- 按钮主体继续取封面第一强调色；缺失或不可用封面时沿用当前回退颜色机制。
- 无曲目时保留现有中性禁用底色、可用性和无障碍语义，不重新定义取色或禁用规则。
- 播放、暂停、加载状态来源、图标、忙碌语义及点击行为保持现状。
- 两种现有 modern 播放条材质使用同一套按钮方向，不新增设置项、偏好或主题分支。

以下不在范围：manuscript PlayerBar、MiniPlayer、全屏播放器、播放引擎、取色算法、图标对比色算法、共享播放逻辑及旧焦点问题。
不承接历史播放加载重试任务，不新增依赖、动画系统、全局 token 或其他控件改造。

## 3. 视觉结构

### 3.1 纽扣式内凹

- 光源固定来自上方，不随鼠标移动，不加入扫光、呼吸或持续发光。
- 外沿是一圈宽而柔的同色环状坡面，表达按钮实体厚度；它不是细边框，也不是整圈高亮描边。
- 中央是同一强调色体系的凹面主体，图标位于几何中心；凹面边界应柔和连续，不出现硬切圆环。
- 内凹的上缘应偏暗，表示中央表面背离上方光源；内凹的下缘应偏亮，表示坡面受光。
- 内上暗、内下亮与中央主体共同形成遮蔽感，正常 40px 尺寸下仍能辨认凹陷方向。
- 明暗仅发生在按钮圆形内部，不越界、不改变轮廓，不借助播放条背景制造“洞”。
- 中央主体仍应像原强调色，不洗白、不显著偏色；环状坡面与主体保持同色关系。

### 3.2 明确禁止

- 禁止 `border`、模拟描边、纽扣孔、细线环或独立整圈等亮光环。
- 禁止 `box-shadow`（包括 `inset`）、`drop-shadow`、外部投影和按钮外的光晕。
- 禁止透明玻璃球、金属纹理、镜面反射、强烈高光点及复杂多层装饰。
- 禁止用明显放大、上浮、缩小图标或整体偏色代替凹面结构。
- 允许使用按钮内部渐变明暗表达坡面与内凹；视觉上的遮蔽感不等于使用 CSS 阴影。

### 3.3 图标与材质关系

沿用现有图标对比色机制。环状坡面和凹面明暗不得盖住播放、暂停或加载图标。
对偏亮、偏暗强调色及回退色，优先收敛表面明暗差，不新增取色算法或共享色彩系统。

## 4. 状态表现

| 状态              | 预期表现                                                                   |
| ----------------- | -------------------------------------------------------------------------- |
| 默认可用          | 外沿略高、中央明确下陷；内上缘暗、内下缘亮，静止即可辨认内凹。             |
| 悬停              | 局部表面轻微响应，可略增强可读性；不显著上浮、放大或改变凹凸方向。         |
| 按住              | 凹面略加深并产生克制压感；不反转明暗，不把内凹变成凸面，也不变成完全平面。 |
| 释放              | 恢复当前对应的默认或悬停内凹外观；不残留位移、缩放或按压状态。             |
| 正在播放 / 已暂停 | 分别沿用现有暂停 / 播放图标与语义，不增加持续发光或另一种材质。            |
| 无曲目            | 沿用中性禁用外观；不响应 hover、active，不产生位移、缩放或提亮。           |
| 加载中            | 沿用既有底色、加载图标及忙碌语义；不新增旋转，也不响应 hover、active。     |

具体渐变幅度、颜色混合和过渡时长留给 TECHDOC 的初始参数；实现时只允许在上述方向内微调。

## 5. 动效与减少动态

仅调整本按钮已有局部 transition，使 hover 与 active 短促、克制并服务于按压反馈。
不引入新动效系统，不加入自动变化、扫光、呼吸或弹跳。
启用减少动态偏好时，不产生本次新增或调整的位移、缩放及相关动画过渡；可保留即时表面状态变化。
disabled 与 busy 状态不响应 hover/active，避免用户误以为控件可操作。

## 6. A 级局部验收

按[风险分级验收](./docs/rules/validation.md)的 A 级纯表现改动处理。

- 检查默认、hover、active、释放四个代表状态：静止明确内凹，按住略加深，释放恢复。
- 用少量偏亮、偏暗强调色及封面回退色确认主体仍为原色体系，图标清楚可读。
- 确认没有 `border`、任何 `box-shadow` / `drop-shadow`、外围光晕、细描边或纽扣孔。
- 确认无曲目与加载状态不产生 hover/active 反馈；检查减少动态时无位移、缩放和相关过渡。
- 在两种现有 modern 播放条材质上快速查看同一区域，确认按钮不漂白、不刺眼，也不依赖背景形成凹洞。
- 通过作用域确认 manuscript、MiniPlayer、全屏播放器及其他按钮不受影响。

不运行全量 test、lint、typecheck、build，不搭建截图平台；无可用 GUI 时如实标注视觉未验证。
完成标准：用户在正常尺寸下能直接看出“同色外沿略高、中央内凹”的纽扣式播放按钮，且颜色、语义与布局不变。
