# Auralis 协作入口

本文件维护 Auralis 的项目边界和规则路由。中文沟通、工具使用、编码、Git 授权和代理协作等
通用约定沿用全局 `AGENTS.md`；专项执行细节由下表对应文件维护。

## 项目边界

- Auralis 是面向个人大型音乐收藏的本地优先播放器，不扩展流媒体、社交、推荐或在线内容。
  功能状态以当前源码、路由和测试为准，不以历史文档的完成度描述为准。
- 只处理用户授权的范围。执行操作前先核对已有授权，范围和目标明确时继续执行，不重复询问。
  删除或递归移动同样先核对已有授权，并按环境规则检查绝对路径与范围；仅在超出授权、目标不明
  或涉及未授权的真实数据操作时询问。其他问题先报告，不顺手修复。
- Renderer 不得直接访问数据库、文件系统和 Electron 主进程能力；跨进程访问必须经过类型化
  Preload/IPC。业务分层与 Service 的引入条件见架构规则。
- 验收与交付统一按风险分级验收规则执行，选择覆盖最终状态主要风险的最小验证集合。

## 规则路由

开始相关工作前，主动读取下表命中的文档；链接不代表内容已经加载。
多项命中时组合读取，仅执行与本次任务相关的条款，不一次加载所有专项规则。
只读问答不触发修改验收；纯文档修改不触发应用架构或 Renderer 规则。

| 触发条件                                                             | 必读规则                                        |
| -------------------------------------------------------------------- | ----------------------------------------------- |
| 执行命令、操作文件、处理中文编码、安装依赖或启动应用                 | [环境与操作](docs/rules/environment.md)         |
| 新建模块、调整源码组织、跨层调用、IPC、主进程服务或日志              | [架构与 IPC](docs/rules/architecture.md)        |
| 曲库分页、快照、搜索消费者、数据库迁移、扫描、标签写回或音乐文件操作 | [曲库与数据](docs/rules/library-data.md)        |
| Renderer 页面、组件、样式、播放界面、视觉状态或窗口几何              | [Renderer 视觉与交互](docs/rules/renderer.md)   |
| 输出供其他会话或 Agent 执行的施工提示词                              | [施工提示词写法](docs/rules/handoff-prompts.md) |
| 修改代码、配置或文档，准备选择验收方式                               | [风险分级验收](docs/rules/validation.md)        |
| 用户要求提交、推送、合并、PR、打包或发布                             | [Git 与发布](docs/rules/git-release.md)         |

例如：调整 modern 播放按钮颜色，读取环境、Renderer 和验收规则；只检查该按钮受影响状态，
不因此读取数据库规则或运行全量测试。若涉及共享播放逻辑，再按实际影响升级验收。

## 适用关系与维护

- 遵守上级指令与当前用户明确要求；本入口规定仓库全局边界，专项规则在其适用范围内细化。
  出现真实冲突时说明冲突，不擅自选择会扩大任务或减弱安全边界的解释。
- 规则单一来源：个人协作习惯放在全局规则，项目边界和路由放在本文件，专项条款放在对应文档。
  新增专项文件时同步增加明确触发条件。
- 规则描述需要保持的约束；具体数值、配置和可变实现引用源码。当前实现说明不作为永久设计限制。
- 不为每个小改动新增规则文件或技术文档。只有可复用的约束才进入规则目录。
- `docs/rules/` 必须可被 Git 跟踪；调整路由后只需检查链接、内容归属和规则是否遗漏。
